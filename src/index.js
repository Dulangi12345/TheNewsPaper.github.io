// window.addEventListener('scroll', function() {
//   var section = document.querySelector('#Catalyst-section');
//   var sectionTop = section.getBoundingClientRect().top;
//   var sectionBottom = section.getBoundingClientRect().bottom;
//   var screenPosition = window.innerHeight;

//   // Check if the bottom of the section is above or at the bottom of the viewport
//   // and if the top of the section is below the top of the viewport
//   if (sectionBottom >= screenPosition ) {
//     section.classList.add('in-view');
//   } else {
//     // Add a delay of 300 milliseconds before removing the 'in-view' class
//     setTimeout(function() {
//       section.classList.add('in-view');
//     }, 30);
//   }
// });



// window.addEventListener('scroll', function() {
//   var section = document.querySelector('#kagura-section');
//   var sectionTop = section.getBoundingClientRect().top;
//   var sectionBottom = section.getBoundingClientRect().bottom;
//   var screenPosition = window.innerHeight;

//   // Check if the bottom of the section is above or at the bottom of the viewport
//   // and if the top of the section is below the top of the viewport
//   if (sectionBottom >= screenPosition ) {
//     section.classList.add('in-view');
//   } else {
//     // Add a delay of 300 milliseconds before removing the 'in-view' class
//     setTimeout(function() {
//       section.classList.add('in-view');
//     }, 30);
//   }
// });



// window.addEventListener('scroll', function() {
//   var section = document.querySelector('#CTT-section');
//   var sectionTop = section.getBoundingClientRect().top;
//   var sectionBottom = section.getBoundingClientRect().bottom;
//   var screenPosition = window.innerHeight;

//   // Check if the bottom of the section is above or at the bottom of the viewport
//   // and if the top of the section is below the top of the viewport
//   if (sectionBottom >= screenPosition ) {
//     section.classList.add('in-view');
//   } else {
//     // Add a delay of 300 milliseconds before removing the 'in-view' class
//     setTimeout(function() {
//       section.classList.add('in-view');
//     }, 30);
//   }
// });



// window.addEventListener('scroll', function() {
//   var section = document.querySelector('#list-motto');
//   var sectionTop = section.getBoundingClientRect().top;
//   var sectionBottom = section.getBoundingClientRect().bottom;
//   var screenPosition = window.innerHeight;

//   // Check if the bottom of the section is above or at the bottom of the viewport
//   // and if the top of the section is below the top of the viewport
//   if (sectionBottom >= screenPosition ) {
//     section.classList.add('in-view');
//   } else {
//     // Add a delay of 300 milliseconds before removing the 'in-view' class
//     setTimeout(function() {
//       section.classList.add('in-view');
//     }, 30);
//   }
// });

// const observer = new IntersectionObserver(entries => {
//   intersections.forEach(({
//     target,
//     isIntersecting
//   }) => {
//     if (isIntersecting) {
//       target.classList.add('animate');
//     } else {
//       target.classList.remove('animate');
//     }

//   });
// }, {
//   threshold: 0
// });

